- **Class Imbalance.** with only one example each of "data scientist" and "machine learning engineer" in the training set (compared to 12 for "data engineer"), teh model struggles to learn these miniority categories and seems to defalt to the majority class. This is an expected result as given the small dataset size and documented here rather than hidden. A larger, more balanced dataset (which can be achieved by running the collection script over more time) would likely improve this.